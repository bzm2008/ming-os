# Allow the active desktop user and the existing video group to control only
# the kernel backlight brightness node. Device ownership remains with root.
SUBSYSTEM=="backlight", ACTION=="add|change", TAG+="uaccess", RUN+="/bin/chgrp video /sys/class/backlight/%k/brightness", RUN+="/bin/chmod g+w /sys/class/backlight/%k/brightness"
